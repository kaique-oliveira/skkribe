<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Skribe.Views.DropZoneView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Padding="24">
        <Border Style="{StaticResource Card}" Padding="40" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="14">
                <FontIcon Glyph="&#xE898;" FontSize="40" Foreground="{StaticResource PurpleBrush}" />
                <TextBlock Text="Transcrever áudio ou vídeo" FontSize="18" FontWeight="SemiBold"
                           HorizontalAlignment="Center" Foreground="{StaticResource TextPrimaryBrush}"/>
                <TextBlock Text="Solte ou escolha um arquivo" FontSize="12"
                           HorizontalAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}"/>
                <TextBlock Text="MP3  ·  M4A  ·  WAV  ·  FLAC  ·  MP4  ·  MOV  ·  M4V"
                           FontSize="11" HorizontalAlignment="Center"
                           Foreground="{StaticResource TextTertiaryBrush}"/>
                <Button x:Name="ChooseFileButton" Style="{StaticResource SecondaryButton}"
                        HorizontalAlignment="Center" Content="Escolher arquivo" Click="ChooseFile_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
